info:
  name: Login
  type: http
  seq: 1

http:
  method: POST
  url: https://localhost:7273/api/Usuarios/login
  body:
    type: json
    data: |-
      {
        "correo": "string",
        "contrasena": "string"
      }
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
