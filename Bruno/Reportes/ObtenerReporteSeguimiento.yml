info:
  name: ObtenerReporteSeguimiento
  type: http
  seq: 3

http:
  method: GET
  url: https://localhost:7273/api/Reportes/seguimiento?top=10
  params:
    - name: top
      value: "10"
      type: query
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
