info:
  name: MisNotificaciones
  type: http
  seq: 10

http:
  method: GET
  url: https://localhost:7273/api/Usuarios/mis-notificaciones
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
